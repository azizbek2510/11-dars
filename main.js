const bgColor = prompt("Какой будет цвет фона?");
let textColor = prompt("Какой будет цвет текста?");

if (bgColor) {
  document.body.style.backgroundColor = bgColor;
}

if (textColor) {
  document.body.style.color = textColor;
}









